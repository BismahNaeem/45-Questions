"use strict";
//They think of something you could store in a TypeScript Object.
//write a program that creates Objects containing these items.
let book = {
    name: "typscript",
    price: 200,
    author: "unknown",
};
console.log(`book info:${book.name} by ${book.author} and its price is  ${book.price}`);
